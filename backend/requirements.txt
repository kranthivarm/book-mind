fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# PostgreSQL
asyncpg==0.29.0

# PDF Processing
PyMuPDF==1.24.10

# CPU-only PyTorch — install BEFORE sentence-transformers
# Prevents pip pulling full GPU version (~800MB → ~200MB)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
torchvision==0.17.2+cpu

# Embeddings + Reranker (uses CPU torch above)
sentence-transformers==3.1.1

# Vector Database
chromadb==0.5.15

# Groq LLM
groq==0.11.0
httpx==0.27.0

# Auth
bcrypt==4.2.1
python-jose[cryptography]==3.3.0

# Utilities
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2
pydantic[email]==2.9.2